<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <%- include('partials/header') %>

        <main>
            <div class="container">
                <h1>Reset Password</h1>

                <!-- Password Reset Modal -->
                <div id="passwordResetModal" class="modal" style="display: block;">
                    <div class="modal-content">
                        <span class="close" id="closeModal">&times;</span>
                        <h2>Forgot Password</h2>

                        <div id="step1" class="modal-step">
                            <h3>Step 1: Verify Your Identity</h3>
                            <form id="step1Form">
                                <div class="form-group">
                                    <label for="username">Username:</label>
                                    <input type="text" id="username" name="username" required>
                                </div>
                                <div class="form-group">
                                    <label for="email">Email Address:</label>
                                    <input type="email" id="email" name="email" required>
                                </div>
                                <button type="submit" class="btn">Send Verification Code</button>
                            </form>
                        </div>

                        <div id="step2" class="modal-step" style="display: none;">
                            <h3>Step 2: Verify Your Email</h3>
                            <p id="verificationEmail" style="color: #16a085; font-weight: bold;"></p>
                            <p style="color: #666; font-size: 0.9rem;">We've sent a verification code to your email
                                address. Enter
                                it below or click the link in the email.</p>

                            <form id="step2Form">
                                <div class="form-group">
                                    <label for="verificationCode">Verification Code:</label>
                                    <input type="text" id="verificationCode" name="verificationCode"
                                        placeholder="e.g., 123456" maxlength="6" required>
                                </div>
                                <div id="verificationInlineError"
                                    style="display: none; color: #e74c3c; font-weight: 600; margin-bottom: 0.75rem;">
                                </div>
                                <div id="previewUrl"
                                    style="margin-bottom: 1rem; padding: 1rem; background: #f0f9f7; border-radius: 4px; display: none;">
                                    <p style="margin: 0 0 0.5rem 0; font-size: 0.85rem; color: #666;">Development: Click
                                        the link
                                        below to preview the verification email:</p>
                                    <a id="previewLink" href="" target="_blank"
                                        style="word-break: break-all; font-size: 0.8rem;">Preview Email</a>
                                </div>
                                <button type="submit" class="btn">Verify Email</button>
                                <button type="button" class="btn btn-secondary" id="resendBtn">Resend Code</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </main>

        <%- include('partials/footer') %>

            <script src="/js/forgot-password.js"></script>
</body>

</html>