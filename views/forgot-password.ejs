<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
        }

        .modal.show {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .modal-content {
            background-color: white;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
            max-width: 400px;
            width: 90%;
        }

        .modal-header {
            margin-bottom: 25px;
        }

        .modal-header h2 {
            margin: 0;
            color: #333;
            font-size: 24px;
        }

        .modal-header p {
            margin: 10px 0 0 0;
            color: #666;
            font-size: 14px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #333;
            font-weight: 500;
            font-size: 14px;
        }

        .form-group input {
            width: 100%;
            padding: 10px 12px;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-size: 14px;
            box-sizing: border-box;
        }

        .form-group input:focus {
            outline: none;
            border-color: #16a085;
            box-shadow: 0 0 0 3px rgba(22, 160, 133, 0.1);
        }

        .error-message {
            color: #e74c3c;
            font-size: 13px;
            margin-top: 5px;
        }

        .success-message {
            color: #27ae60;
            font-size: 13px;
            margin-top: 5px;
        }

        .button-group {
            display: flex;
            gap: 10px;
            margin-top: 25px;
        }

        .btn {
            flex: 1;
            padding: 11px 15px;
            border: none;
            border-radius: 4px;
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.2s;
        }

        .btn-primary {
            background-color: #16a085;
            color: white;
        }

        .btn-primary:hover {
            background-color: #138d75;
        }

        .btn-primary:disabled {
            background-color: #bdc3c7;
            cursor: not-allowed;
        }

        .btn-secondary {
            background-color: #ecf0f1;
            color: #333;
        }

        .btn-secondary:hover {
            background-color: #d5dbdb;
        }

        .verification-code-display {
            display: none;
            background: #f0f0f0;
            padding: 15px;
            border-radius: 4px;
            margin: 15px 0;
            text-align: center;
        }

        .verification-code-display p {
            margin: 0 0 10px 0;
            color: #666;
            font-size: 13px;
        }

        .code {
            font-size: 24px;
            font-weight: bold;
            color: #16a085;
            letter-spacing: 2px;
            font-family: monospace;
        }

        .step-indicator {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            font-size: 12px;
            color: #999;
        }

        .step {
            text-align: center;
        }

        .step.active {
            color: #16a085;
            font-weight: 600;
        }

        .step-separator {
            flex: 1;
            height: 2px;
            background-color: #ecf0f1;
            margin: 0 10px;
        }

        .step-separator.active {
            background-color: #16a085;
        }

        .hidden {
            display: none;
        }
    </style>
</head>

<body>
    <!-- Password Reset Modal -->
    <div id="passwordResetModal" class="modal show">
        <div class="modal-content">
            <div class="step-indicator">
                <div class="step active" id="step1Indicator">1. User Info</div>
                <div class="step-separator active" id="step1Separator"></div>
                <div class="step" id="step2Indicator">2. Verify Code</div>
            </div>

            <!-- Step 1: Username and Email -->
            <form id="step1Form" class="form">
                <div class="modal-header">
                    <h2>Forgot Password</h2>
                    <p>Enter your account information to reset your password</p>
                </div>

                <div class="form-group">
                    <label for="username">Username</label>
                    <input type="text" id="username" name="username" placeholder="Enter your username" required>
                    <div id="step1Error" class="error-message"></div>
                </div>

                <div class="form-group">
                    <label for="email">Email Address</label>
                    <input type="email" id="email" name="email" placeholder="Enter your email address" required>
                </div>

                <div class="button-group">
                    <a href="/" class="btn btn-secondary">Cancel</a>
                    <button type="submit" class="btn btn-primary">Send Verification Code</button>
                </div>
            </form>

            <!-- Step 2: Verification Code -->
            <form id="step2Form" class="form hidden">
                <div class="modal-header">
                    <h2>Verify Your Identity</h2>
                    <p>Enter the 6-digit code sent to your email</p>
                </div>

                <div class="verification-code-display" id="codeDisplay">
                    <p>For development/testing, your code is:</p>
                    <div class="code" id="displayCode"></div>
                </div>

                <div class="form-group">
                    <label for="verificationCode">Verification Code</label>
                    <input type="text" id="verificationCode" name="verification_code" placeholder="Enter 6-digit code"
                        maxlength="6" pattern="[0-9]{6}" required>
                    <div id="step2Error" class="error-message"></div>
                </div>

                <div class="button-group">
                    <button type="button" class="btn btn-secondary" id="backBtn">Back</button>
                    <button type="submit" class="btn btn-primary">Verify & Continue</button>
                </div>

                <p style="text-align: center; margin-top: 15px; color: #999; font-size: 13px;">
                    Didn't receive the code?
                    <a href="#" id="resendBtn" style="color: #16a085; text-decoration: none;">Resend</a>
                </p>
            </form>
        </div>
    </div>

    <script src="/js/forgot-password.js"></script>
</body>

</html>