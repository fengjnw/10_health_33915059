<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body data-user-logged-in="<%= user ? 'true' : 'false' %>">
    <%- include('partials/header') %>

        <main>
            <%- body %>
        </main>

        <%- include('partials/footer') %>

            <!-- Session Timeout Handler - Only load for logged-in users -->
            <% if (user) { %>
                <script src="/js/modules/common/session-timeout.js"></script>
                <% } %>
</body>

</html>