<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <%- include('partials/header') %>

        <main>
            <div class="container">
                <h1>My Activities</h1>

                <div class="activities-header">
                    <a href="/add-activity" class="btn btn-primary">Add New Activity</a>
                    <% if (activities && activities.length> 0) { %>
                        <button id="toggle-filters" class="btn btn-secondary">
                            <span id="filter-icon">▼</span> Advanced Filters
                        </button>
                        <% } %>
                </div>

                <% if (activities && activities.length> 0) { %>
                    <div id="advanced-filters" class="advanced-filters" style="display: none;">
                        <div class="filter-row">
                            <div class="filter-group">
                                <label for="activity-type-filter">Activity Type:</label>
                                <select id="activity-type-filter">
                                    <option value="all" <%=filterParams && filterParams.activity_type==='' ? 'selected'
                                        : '' %>>All Activities</option>
                                    <option value="Running" <%=filterParams && filterParams.activity_type==='Running'
                                        ? 'selected' : '' %>>Running</option>
                                    <option value="Cycling" <%=filterParams && filterParams.activity_type==='Cycling'
                                        ? 'selected' : '' %>>Cycling</option>
                                    <option value="Swimming" <%=filterParams && filterParams.activity_type==='Swimming'
                                        ? 'selected' : '' %>>Swimming</option>
                                    <option value="Gym" <%=filterParams && filterParams.activity_type==='Gym'
                                        ? 'selected' : '' %>>Gym</option>
                                    <option value="Yoga" <%=filterParams && filterParams.activity_type==='Yoga'
                                        ? 'selected' : '' %>>Yoga</option>
                                    <option value="Walking" <%=filterParams && filterParams.activity_type==='Walking'
                                        ? 'selected' : '' %>>Walking</option>
                                    <option value="Hiking" <%=filterParams && filterParams.activity_type==='Hiking'
                                        ? 'selected' : '' %>>Hiking</option>
                                    <option value="Other" <%=filterParams && filterParams.activity_type==='Other'
                                        ? 'selected' : '' %>>Other</option>
                                </select>
                            </div>

                            <div class="filter-group">
                                <label for="date-from-filter">Date From:</label>
                                <input type="date" id="date-from-filter"
                                    value="<%= filterParams ? filterParams.date_from : '' %>">
                            </div>

                            <div class="filter-group">
                                <label for="date-to-filter">Date To:</label>
                                <input type="date" id="date-to-filter"
                                    value="<%= filterParams ? filterParams.date_to : '' %>">
                            </div>
                        </div>

                        <div class="filter-row">
                            <div class="filter-group">
                                <label for="duration-min-filter">Min Duration (min):</label>
                                <input type="number" id="duration-min-filter" min="0" placeholder="e.g., 30"
                                    value="<%= filterParams ? filterParams.duration_min : '' %>">
                            </div>

                            <div class="filter-group">
                                <label for="duration-max-filter">Max Duration (min):</label>
                                <input type="number" id="duration-max-filter" min="0" placeholder="e.g., 120"
                                    value="<%= filterParams ? filterParams.duration_max : '' %>">
                            </div>

                            <div class="filter-group">
                                <label for="calories-min-filter">Min Calories:</label>
                                <input type="number" id="calories-min-filter" min="0" placeholder="e.g., 100"
                                    value="<%= filterParams ? filterParams.calories_min : '' %>">
                            </div>

                            <div class="filter-group">
                                <label for="calories-max-filter">Max Calories:</label>
                                <input type="number" id="calories-max-filter" min="0" placeholder="e.g., 1000"
                                    value="<%= filterParams ? filterParams.calories_max : '' %>">
                            </div>
                        </div>

                        <div class="filter-actions">
                            <button id="apply-filters" class="btn btn-primary btn-small">Apply Filters</button>
                            <button id="clear-filters" class="btn btn-secondary btn-small">Clear All</button>
                        </div>
                    </div>
                    <% } %>

                        <% if (locals.error) { %>
                            <div class="alert alert-error">
                                <%= error %>
                            </div>
                            <% } %>

                                <% if (activities && activities.length> 0) { %>
                                    <input type="hidden" id="csrf-token" value="<%= csrfToken %>">
                                    <div class="results-bar">
                                        <p class="results-count">
                                            Showing <%= activities.length %> of
                                                <%= pagination ? pagination.totalCount : activities.length %>
                                                    <%= (pagination ? pagination.totalCount : activities.length)===1
                                                        ? 'activity' : 'activities' %>
                                        </p>
                                        <div class="results-actions">
                                            <button id="export-activities" class="btn btn-secondary btn-small"
                                                title="Export filtered activities to CSV">
                                                Export CSV
                                            </button>
                                            <div class="sort-controls">
                                                <label for="sort-by">Sort</label>
                                                <select id="sort-by">
                                                    <option value="date-desc">Time (newest)</option>
                                                    <option value="date-asc">Time (oldest)</option>
                                                    <option value="calories-desc">Calories (high→low)</option>
                                                    <option value="calories-asc">Calories (low→high)</option>
                                                    <option value="duration-desc">Duration (long→short)</option>
                                                    <option value="duration-asc">Duration (short→long)</option>
                                                </select>
                                            </div>
                                            <div class="sort-controls">
                                                <label for="activities-pageSize">Per Page</label>
                                                <select id="activities-pageSize">
                                                    <option value="10" <% if (pagination && pagination.pageSize===10) {
                                                        %>selected<% } %>>10</option>
                                                    <option value="20" <% if (pagination && pagination.pageSize===20) {
                                                        %>selected<% } %>>20</option>
                                                    <option value="30" <% if (pagination && pagination.pageSize===30) {
                                                        %>selected<% } %>>30</option>
                                                    <option value="50" <% if (pagination && pagination.pageSize===50) {
                                                        %>selected<% } %>>50</option>
                                                </select>
                                            </div>
                                            <% if (pagination && pagination.totalPages> 1) { %>
                                                <div class="pagination">
                                                    <% const currentPage=pagination.page; %>
                                                        <% const totalPages=pagination.totalPages; %>
                                                            <% const pageSize=pagination.pageSize; %>
                                                                <% const pageLink=(p)=>
                                                                    `/my-activities?page=${p}&pageSize=${pageSize}`; %>

                                                                    <% if (currentPage> 1) { %>
                                                                        <a class="page-link"
                                                                            href="<%= pageLink(currentPage - 1) %>">Prev</a>
                                                                        <% } %>

                                                                            <% for (let p=1; p <=totalPages; p++) { %>
                                                                                <a class="page-link <%= p === currentPage ? 'active' : '' %>"
                                                                                    href="<%= pageLink(p) %>">
                                                                                    <%= p %>
                                                                                </a>
                                                                                <% } %>

                                                                                    <% if (currentPage < totalPages) {
                                                                                        %>
                                                                                        <a class="page-link"
                                                                                            href="<%= pageLink(currentPage + 1) %>">Next</a>
                                                                                        <% } %>
                                                </div>
                                                <% } %>
                                        </div>
                                    </div>
                                    <table class="activities-table">
                                        <thead>
                                            <tr>
                                                <th>Date</th>
                                                <th>Activity Type</th>
                                                <th>Duration (min)</th>
                                                <th>Distance (km)</th>
                                                <th>Calories</th>
                                                <th>Notes</th>
                                                <th>Status</th>
                                                <th>Actions</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% activities.forEach(activity=> { %>
                                                <tr data-activity-type="<%= activity.activity_type %>"
                                                    data-duration="<%= activity.duration_minutes || 0 %>"
                                                    data-distance="<%= activity.distance_km || 0 %>"
                                                    data-calories="<%= activity.calories_burned || 0 %>"
                                                    data-activity-time="<%= activity.activity_time ? new Date(activity.activity_time).toISOString() : '' %>">
                                                    <td>
                                                        <%= activity.activity_time ? new
                                                            Date(activity.activity_time).toLocaleString() : 'N/A' %>
                                                    </td>
                                                    <td>
                                                        <%= activity.activity_type %>
                                                    </td>
                                                    <td>
                                                        <%= activity.duration_minutes %>
                                                    </td>
                                                    <td>
                                                        <%= activity.distance_km || 'N/A' %>
                                                    </td>
                                                    <td>
                                                        <%= activity.calories_burned || 'N/A' %>
                                                    </td>
                                                    <td>
                                                        <%= activity.notes || '-' %>
                                                    </td>
                                                    <td>
                                                        <% if (activity.is_public) { %>
                                                            <span class="status-badge status-public">Public</span>
                                                            <% } else { %>
                                                                <span class="status-badge status-private">Private</span>
                                                                <% } %>
                                                    </td>
                                                    <td>
                                                        <div style="display: flex; gap: 8px;">
                                                            <a href="/my-activities/<%= activity.id %>/edit"
                                                                class="btn btn-small"
                                                                style="flex: 1; display: flex; align-items: center; justify-content: center;">Edit</a>
                                                            <button class="btn btn-small btn-delete delete-btn"
                                                                style="flex: 1;"
                                                                data-activity-id="<%= activity.id %>">Delete</button>
                                                        </div>
                                                    </td>
                                                </tr>
                                                <% }); %>
                                        </tbody>
                                    </table>

                                    <% if (stats) { %>
                                        <div class="activity-stats">
                                            <h2>Statistics</h2>
                                            <div class="stats-grid">
                                                <div class="stat-card">
                                                    <h3>Activities</h3>
                                                    <div class="stat-value">
                                                        <span class="stat-visible">
                                                            <%= stats.total_count %>
                                                        </span>
                                                    </div>
                                                </div>
                                                <div class="stat-card">
                                                    <h3>Duration</h3>
                                                    <div class="stat-value">
                                                        <span class="stat-visible">
                                                            <%= stats.total_duration %>
                                                        </span>
                                                    </div>
                                                    <div class="stat-meta">min</div>
                                                </div>
                                                <div class="stat-card">
                                                    <h3>Distance</h3>
                                                    <div class="stat-value">
                                                        <span class="stat-visible">
                                                            <%= Number(stats.total_distance).toFixed(2) %>
                                                        </span>
                                                    </div>
                                                    <div class="stat-meta">km</div>
                                                </div>
                                                <div class="stat-card">
                                                    <h3>Calories</h3>
                                                    <div class="stat-value">
                                                        <span class="stat-visible">
                                                            <%= stats.total_calories %>
                                                        </span>
                                                    </div>
                                                    <div class="stat-meta">cal</div>
                                                </div>
                                            </div>
                                        </div>
                                        <% } %>
                                            <% } else { %>
                                                <div class="no-activities">
                                                    <p>You haven't logged any activities yet.</p>
                                                    <a href="/add-activity" class="btn btn-primary">Add Your First
                                                        Activity</a>
                                                </div>
                                                <% } %>
            </div>
        </main>

        <%- include('partials/footer') %>

            <script src="/js/utils/sort-helper.js"></script>
            <script src="/js/delete-activity.js"></script>
            <script src="/js/filter-activities.js"></script>
            <script src="/js/export-activities.js"></script>
            <script src="/js/page-size-selector.js"></script>
</body>

</html>