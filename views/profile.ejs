<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <%- include('partials/header') %>

        <main>
            <div class="container profile-page">
                <h1>My Profile</h1>

                <div class="form-container profile-card">
                    <div class="account-card">
                        <div class="account-card__title">Account</div>
                        <div class="account-card__meta">Member Since: <%= new Date(user.created_at).toLocaleString() %>
                        </div>
                        <div class="account-card__actions flex-gap-sm">
                            <a href="/auth/change-password" class="btn btn-secondary btn-compact">Change Password</a>
                            <button type="button" id="deleteAccountBtn" class="btn btn-danger btn-compact">Delete
                                Account</button>
                        </div>
                    </div>

                    <div id="message-container"></div>

                    <form id="profile-form" class="activity-form">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                        <div class="form-group">
                            <label for="username">Username:</label>
                            <input type="text" id="username" name="username" value="<%= user.username %>" minlength="3"
                                maxlength="50" pattern=".{3,50}" required>
                            <span class="form-help muted">3-50 characters, must be unique</span>
                        </div>

                        <div class="form-row">
                            <div class="form-group">
                                <label for="first_name">First Name:</label>
                                <input type="text" id="first_name" name="first_name"
                                    value="<%= user.first_name || '' %>" required>
                            </div>

                            <div class="form-group">
                                <label for="last_name">Last Name:</label>
                                <input type="text" id="last_name" name="last_name" value="<%= user.last_name || '' %>"
                                    required>
                            </div>
                        </div>

                        <div class="form-group">
                            <label for="email">Email:</label>
                            <div class="flex-gap-sm flex-align">
                                <input type="email" id="email" name="email" value="<%= user.email %>" disabled readonly
                                    class="flex-1">
                                <button type="button" id="changeEmailBtn" class="btn btn-secondary btn-compact">Change
                                    Email</button>
                            </div>
                        </div>

                        <div class="form-group flex-gap-12">
                            <button type="submit" class="btn btn-primary flex-1">Update Profile</button>
                            <a href="/my-activities" class="btn btn-secondary flex-1 flex-center">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </main>

        <%- include('partials/footer') %>

            <%- include('partials/change-email-modal') %>

                <!-- Delete Account Modal -->
                <div id="deleteAccountModal" class="modal">
                    <div class="modal-content">
                        <span class="close">&times;</span>

                        <!-- Step 1: Confirm and request code -->
                        <div id="deleteStep1">
                            <h3>Delete Account</h3>
                            <p class="warning-text">⚠️ Warning: This action cannot be undone!</p>
                            <p>All your data including activities and profile information will be permanently deleted.
                            </p>
                            <form id="deleteRequestCodeForm">
                                <div class="error-message" id="deleteRequestError" style="display: none;"></div>
                                <div class="form-group flex-gap-12">
                                    <button type="submit" class="btn btn-danger flex-1">Send Verification Code</button>
                                    <button type="button" id="deleteCancelBtn"
                                        class="btn btn-secondary flex-1">Cancel</button>
                                </div>
                            </form>
                        </div>

                        <!-- Step 2: Verify code -->
                        <div id="deleteStep2" style="display: none;">
                            <h3>Verify Deletion</h3>
                            <p>A verification code has been sent to your email. Enter it below to confirm account
                                deletion.</p>
                            <form id="deleteVerifyForm">
                                <div class="form-group">
                                    <label for="deleteVerificationCode">Verification Code:</label>
                                    <input type="text" id="deleteVerificationCode" required maxlength="8"
                                        autocomplete="one-time-code">
                                </div>
                                <div class="error-message" id="deleteVerificationError" style="display: none;"></div>
                                <div class="form-group flex-gap-12">
                                    <button type="submit" class="btn btn-danger flex-1">Confirm Delete Account</button>
                                    <button type="button" id="deleteBackBtn"
                                        class="btn btn-secondary flex-1">Back</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>

                <!-- Profile update success modal -->
                <div id="profileSuccessModal" class="modal profile-success hidden">
                    <div class="modal-content center">
                        <h3>Profile Updated</h3>
                        <p class="text-muted">Your profile information has been saved.</p>
                    </div>
                </div>

                <script src="/js/utils/csrf-helper.js"></script>
                <script src="/js/utils/form-helper.js"></script>
                <script src="/js/modules/account/profile.js"></script>
                <script src="/js/modules/account/change-email.js"></script>
                <script src="/js/modules/account/delete-account.js"></script>
</body>

</html>