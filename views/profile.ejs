<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <%- include('partials/header') %>

        <main>
            <div class="container">
                <h1>My Profile</h1>

                <div class="form-container">
                    <div
                        style="display: grid; gap: 0.4rem; padding: 1rem; background: #f8fafb; border: 1px solid #d5e8e4; border-radius: 8px; margin-bottom: 1rem;">
                        <div style="font-weight: 600; color: #16a085;">Account</div>
                        <div style="color: #34495e;">Member Since: <%= new Date(user.created_at).toLocaleDateString() %>
                        </div>
                        <div style="display: flex; flex-wrap: wrap; gap: 0.75rem; align-items: center;">
                            <a href="/auth/change-password"
                                style="color: #16a085; font-weight: 600; text-decoration: none;">Change Password</a>
                        </div>
                    </div>

                    <div id="message-container"></div>

                    <form id="profile-form" class="activity-form">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                        <div class="form-group">
                            <label for="username">Username:</label>
                            <input type="text" id="username" name="username" value="<%= user.username %>" disabled
                                readonly>
                            <span class="form-help">Username cannot be changed</span>
                        </div>

                        <div class="form-row">
                            <div class="form-group">
                                <label for="first_name">First Name:</label>
                                <input type="text" id="first_name" name="first_name"
                                    value="<%= user.first_name || '' %>" required>
                            </div>

                            <div class="form-group">
                                <label for="last_name">Last Name:</label>
                                <input type="text" id="last_name" name="last_name" value="<%= user.last_name || '' %>"
                                    required>
                            </div>
                        </div>

                        <div class="form-group">
                            <label for="email">Email:</label>
                            <div style="display: flex; gap: 0.5rem; align-items: center;">
                                <input type="email" id="email" name="email" value="<%= user.email %>" disabled readonly
                                    style="flex: 1;">
                                <button type="button" id="changeEmailBtn" class="btn btn-secondary"
                                    style="margin: 0;">Change Email</button>
                            </div>
                            <span class="form-help">Use the button to start email verification</span>
                        </div>

                        <div class="form-group" style="display: flex; gap: 12px;">
                            <button type="submit" class="btn btn-primary" style="flex: 1;">Update Profile</button>
                            <a href="/my-activities" class="btn btn-secondary"
                                style="flex: 1; display: flex; align-items: center; justify-content: center;">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </main>

        <%- include('partials/footer') %>

            <%- include('partials/change-email-modal') %>

                <script src="/js/profile.js"></script>
                <script src="/js/change-email.js"></script>
</body>

</html>